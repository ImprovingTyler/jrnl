import styled from 'styled-components'

const S = {}
S.CreateMenu = styled.div`
    width: 100%;
    height: 100%;
    color: white;
`;

const CreateMenu = () => {

    return (
        <S.CreateMenu>
            CREATE MENU
        </S.CreateMenu>
    )
}

export default CreateMenu