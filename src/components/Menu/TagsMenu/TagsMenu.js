import styled from 'styled-components'

const S = {}
S.TagsMenu = styled.div`
    width: 100%;
    height: 100%;
    color: white;
`;

const TagsMenu = () => {

    return (
        <S.TagsMenu>
            TAGS MENU
        </S.TagsMenu>
    )
}

export default TagsMenu